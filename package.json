{
  "name": "litematica-builder",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit && vitest run --passWithNoTests && vite build"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "three": "^0.171.0"
  }
}
